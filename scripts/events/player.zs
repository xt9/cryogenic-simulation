#import crafttweaker.event.PlayerLoggedInEvent;

/* events.onPlayerLoggedIn(function(event as PlayerLoggedInEvent) {
    # Player has already reached the Blood Magic stage, return
    if(event.player.hasGameStage("bm_stage")) {
        return;
    } else if(!event.player.hasGameStage("pre_bm_stage")) {
        event.player.addGameStage("pre_bm_stage");
    }
}); */
